-- ✅ MẪU CHUẨN CHO NGƯỜI MỚI | CUN HUB (BẢN RÚT GỌN)
repeat task.wait() until game:IsLoaded()

-- Nút mở menu
local gui = Instance.new("ScreenGui", game.CoreGui)
local btn = Instance.new("ImageButton", gui)
Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 10)
btn.BackgroundColor3 = Color3.fromRGB(0,0,0)
btn.Position = UDim2.new(0.1,0,0.16,0)
btn.Size = UDim2.new(0,40,0,40)
btn.Draggable = true
btn.Image = "" -- Đổi ID ảnh tại đây

btn.MouseButton1Down:Connect(function()
	game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)

-- Giao diện Fluent
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
	Title = "Cun HUB =}",
	SubTitle = "tonghop",
	TabWidth = 157,
	Size = UDim2.fromOffset(450, 300),
	Acrylic = true,
	Theme = "Dark",
	MinimizeKey = Enum.KeyCode.End
})

local Tabs = {
	Main0 = Window:AddTab({ Title = "thối não" }),
	Main1 = Window:AddTab({ Title = "script hỗ trợ" }),
	Main2 = Window:AddTab({ Title = "sigmaboy" }),
}

-- Tab 1: Tiktok
Tabs.Main0:AddButton({
	Title = "tiktok bạn:)",
	Description = "oách xà lách vô cùng",
	Callback = function() setclipboard("https://www.tiktok.com/@qqqq69856") end
})
Tabs.Main0:AddButton({
	Title = "tiktok",
	Description = "fl tiktok đi",
	Callback = function() setclipboard("https://www.tiktok.com/@b.yt03?_t=ZS-90QsHmSjaiu&_r=1") end
})

-- Tab 2: Thêm script
local function addScript(tab, title, desc, url)
	tab:AddButton({
		Title = title,
		Description = desc,
		Callback = function()
			loadstring(game:HttpGet(url))()
		end
	})
end

addScript(Tabs.Main1, "Mỉanda", "script Mỉanda", "https://pastefy.app/JJVhs3rK/raw")
addScript(Tabs.Main1, "LEMON", "SCRIPT LEMON", "https://pastefy.app/MJw2J4T6/raw")
addScript(Tabs.Main1, "CHILI HUB", "SCRIPT CHILI HUB", "https://raw.githubusercontent.com/tienkhanh1/spicy/main/Chilli.lua")
addScript(Tabs.Main1, "zzZ", "ZZz", "https://pastefy.app/JMbCcLIP/raw")
addScript(Tabs.Main2, "SEVER VIP FREE", "SEVER VIP FREE", "https://raw.githubusercontent.com/tienkhanh1/spicy/main/PrivateServer")
addScript(Tabs.Main2, "zzzZ TOOL", "ZZZ TOOL", "https://pandadevelopment.net/virtual/file/570a5fa7765cbff8")
